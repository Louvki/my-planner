const initState = {
    areas: [
        {id: 1, name: "test1"},
        {id: 2, name: "test2"},
    ]
} 

const areaReducer = (state = initState, action) => {
    return state;
}

export default areaReducer;